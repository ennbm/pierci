require ('dotenv').config;
const express = require ('express');
const mysql = require ('mysql2/promise');
const jwt = require ('jsonwebtoken');
const bcrtypt = require ('bcrtypt');
const { configDotenv } = require('dotenv');
